(()=>{
let multi=[];
const types=[['bar','Bar'],['horizontal','Horizontal Bar'],['line','Line'],['area','Area'],['stacked','Stacked'],['stackedArea','Stacked Area'],['pie','Pie'],['doughnut','Doughnut'],['polarArea','Polar Area'],['radar','Radar'],['scatter','Scatter'],['bubble','Bubble'],['histogram','Histogram']];
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
const nval=v=>{if(typeof v==='number')return Number.isFinite(v)?v:NaN;const s=String(v??'').replace(/,/g,'').replace(/%/g,'').trim();if(!s)return NaN;const n=Number(s);return Number.isFinite(n)?n:NaN};
function aggregate(a,x,ys,agg,limit){const source=a.data||[];if(x===null||x===undefined||x<0){return [{label:'Total',...Object.fromEntries(ys.map((y,k)=>['v'+k,calc(source,y,agg)]))}]}
const groups=new Map();source.forEach(r=>{const key=String(r[x]??'').trim()||'(Blank)';if(!groups.has(key))groups.set(key,[]);groups.get(key).push(r)});
let out=[...groups.entries()].map(([label,rs])=>({label,...Object.fromEntries(ys.map((y,k)=>['v'+k,calc(rs,y,agg)]))}));
if(agg==='sum'||agg==='count')out.sort((p,q)=>(q.v0||0)-(p.v0||0));return out.slice(0,limit||20)}
function calc(rows,y,agg){const vals=rows.map(r=>nval(r[y])).filter(Number.isFinite);if(agg==='count')return vals.length;if(!vals.length)return 0;if(agg==='avg')return vals.reduce((a,b)=>a+b,0)/vals.length;if(agg==='min')return Math.min(...vals);if(agg==='max')return Math.max(...vals);return vals.reduce((a,b)=>a+b,0)}
function currentAnalysis(){try{return typeof lastAnalysis!=='undefined'?lastAnalysis:null}catch{return null}}
function mount(){const r=document.getElementById('results');if(!r)return;const old=document.getElementById('multiChartTools');if(old)old.remove();multi=[];const box=document.createElement('div');box.id='multiChartTools';box.className='pro-tools';box.innerHTML=`<div class="pro-tool-head"><div><p class="eyebrow">PRO CHART BUILDER</p><h3>Build multiple charts</h3><p class="muted">Add Chart 1, Chart 2, Chart 3 and more. Every chart can use different fields, calculations and styles.</p></div><button class="btn primary" id="addChartBtn" type="button">＋ Add Chart</button></div><div id="chartBuilderList"></div><div id="multiChartsHost" class="charts-grid"></div>`;r.appendChild(box);document.getElementById('addChartBtn').onclick=addChart;addChart();ensureExports()}
function fieldOptions(a){return a.headers.map((h,i)=>`<option value="${i}">${esc(h)}</option>`).join('')}
function numericOptions(a){return a.numericCols.map(i=>`<option value="${i}">${esc(a.headers[i])}</option>`).join('')}
function addChart(){const a=currentAnalysis();if(!a||!a.numericCols?.length)return;const id=Date.now()+Math.random();const c={id,type:'bar',x:-1,ys:[a.numericCols[0]],agg:'sum',limit:20};multi.push(c);const el=document.createElement('div');el.className='chart-builder-card';el.dataset.id=id;el.innerHTML=`<div class="builder-title"><b>Chart ${multi.length}</b><button class="btn small danger" type="button" data-remove>Remove</button></div><div class="builder-grid"><label>X-axis / Category<select data-x><option value="-1">Entire dataset</option>${fieldOptions(a)}</select></label><label>Data fields<select data-y multiple size="4">${numericOptions(a)}</select><small>Use Ctrl/Cmd-click to select multiple numeric fields.</small></label><label>Chart type<select data-type>${types.map(([v,l])=>`<option value="${v}">${l}</option>`).join('')}</select></label><label>Calculation<select data-agg><option value="sum">Sum / Total</option><option value="avg">Average</option><option value="min">Minimum</option><option value="max">Maximum</option><option value="count">Count</option></select></label><label>Top categories<select data-limit><option>10</option><option selected>20</option><option>50</option><option>100</option></select></label></div><button class="btn primary" data-generate type="button">Generate / Update Chart</button>`;document.getElementById('chartBuilderList').appendChild(el);el.querySelector('[data-y]').value=String(c.ys[0]);el.querySelector('[data-type]').value=c.type;el.querySelector('[data-x]').value=String(c.x);el.querySelector('[data-remove]').onclick=()=>{multi=multi.filter(x=>x.id!==id);el.remove();renumber();renderAll();};el.querySelector('[data-generate]').onclick=()=>{sync(el,c);renderAll();};sync(el,c);renderAll();ensureExports()}
function renumber(){document.querySelectorAll('.chart-builder-card').forEach((e,i)=>e.querySelector('.builder-title b').textContent=`Chart ${i+1}`)}
function sync(el,c){c.x=Number(el.querySelector('[data-x]').value);c.ys=[...el.querySelector('[data-y]').selectedOptions].map(o=>Number(o.value));c.type=el.querySelector('[data-type]').value;c.agg=el.querySelector('[data-agg]').value;c.limit=Number(el.querySelector('[data-limit]').value)||20}
function syncAll(){document.querySelectorAll('.chart-builder-card').forEach(el=>{const c=multi.find(x=>String(x.id)===String(el.dataset.id));if(c)sync(el,c)})}
function renderAll(){const a=currentAnalysis(),host=document.getElementById('multiChartsHost');if(!a||!host)return;syncAll();host.innerHTML='';multi.forEach((c,i)=>renderOne(a,c,i+1));ensureExports()}
function renderOne(a,c,num){if(!c.ys.length)return;const rows=aggregate(a,c.x<0?null:c.x,c.ys,c.agg,c.limit),labels=rows.map(r=>r.label);const wrap=document.createElement('div');wrap.className='chart-box chart-pro';wrap.innerHTML=`<div class="chart-head"><div><h3>Chart ${num} — ${esc(a.headers[c.ys[0]])}</h3><p class="muted">${esc(types.find(t=>t[0]===c.type)?.[1]||c.type)} • ${esc(c.agg)} • ${c.x<0?'Entire dataset':esc(a.headers[c.x])}</p></div><button class="btn small" type="button" data-png>Download PNG</button></div><canvas height="330"></canvas>`;document.getElementById('multiChartsHost').appendChild(wrap);const cv=wrap.querySelector('canvas');let type=c.type;if(type==='horizontal'||type==='histogram')type='bar';if(type==='area'||type==='stackedArea')type='line';const datasets=c.ys.map((yi,k)=>({label:a.headers[yi],data:rows.map(r=>r['v'+k]),backgroundColor:`hsl(${145+k*55} 55% 50% / .65)`,borderColor:`hsl(${145+k*55} 55% 50%)`,borderWidth:2,fill:c.type.includes('Area'),tension:.25}));let cfg={type,data:{labels,datasets},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:datasets.length>1}},scales:{x:{stacked:c.type.includes('stacked')},y:{stacked:c.type.includes('stacked'),beginAtZero:true}}}};if(c.type==='horizontal')cfg.options.indexAxis='y';if(['pie','doughnut','polarArea'].includes(c.type))cfg={type,data:{labels,datasets:[{...datasets[0],backgroundColor:labels.map((_,i)=>`hsl(${145+i*31} 55% ${42+(i%3)*7}%)`)}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{position:'right'}}}};if(c.type==='radar')cfg={type:'radar',data:{labels,datasets:[datasets[0]]},options:{responsive:true,maintainAspectRatio:false,scales:{r:{beginAtZero:true}}}};if(c.type==='scatter'||c.type==='bubble'){let xi=c.x<0?a.numericCols.find(i=>i!==c.ys[0]):c.x;if(xi===undefined)xi=c.ys[0];const yi=c.ys[0];const pts=a.data.map(r=>({x:nval(r[xi]),y:nval(r[yi]),...(c.type==='bubble'?{r:Math.max(3,Math.min(18,Math.sqrt(Math.abs(nval(r[yi]))||1)/4))}:{})})).filter(p=>Number.isFinite(p.x)&&Number.isFinite(p.y));cfg={type:c.type,data:{datasets:[{...datasets[0],data:pts}]},options:{responsive:true,maintainAspectRatio:false,scales:{x:{type:'linear'},y:{beginAtZero:true}}}}}if(c.type==='histogram'){const vals=a.data.map(r=>nval(r[c.ys[0]])).filter(Number.isFinite);if(vals.length){const min=Math.min(...vals),max=Math.max(...vals),bins=Math.min(15,Math.max(5,Math.ceil(Math.sqrt(vals.length)))),step=(max-min||1)/bins,counts=Array(bins).fill(0);vals.forEach(v=>counts[Math.min(bins-1,Math.floor((v-min)/step))]++);cfg={type:'bar',data:{labels:counts.map((_,i)=>`${(min+i*step).toFixed(1)}–${(min+(i+1)*step).toFixed(1)}`),datasets:[{label:a.headers[c.ys[0]],data:counts}]},options:{responsive:true,maintainAspectRatio:false}}}}const chart=new Chart(cv.getContext('2d'),cfg);cv._proChart=chart;wrap.querySelector('[data-png]').onclick=()=>downloadCanvas(cv,`DataInsightPro_Chart_${num}.png`)}
function downloadCanvas(cv,name){const a=document.createElement('a');a.href=cv.toDataURL('image/png',1);a.download=name;document.body.appendChild(a);a.click();a.remove()}
function ensureExports(){const b=document.querySelector('.export-row');if(!b||document.getElementById('downloadAllChartsPDF'))return;const pdf=document.createElement('button');pdf.id='downloadAllChartsPDF';pdf.className='btn';pdf.type='button';pdf.textContent='Download All Charts (PDF)';pdf.onclick=downloadAllPdf;b.appendChild(pdf);const excel=document.createElement('button');excel.id='downloadChartsExcel';excel.className='btn';excel.type='button';excel.textContent='Download Analysis Excel';excel.onclick=downloadAnalysisExcel;b.appendChild(excel);const zip=document.createElement('button');zip.id='downloadAllChartsZip';zip.className='btn';zip.type='button';zip.textContent='Download All PNGs (ZIP)';zip.onclick=downloadZip;b.appendChild(zip)}
async function downloadAllPdf(){syncAll();renderAll();const canvases=[...document.querySelectorAll('#multiChartsHost canvas')];if(!canvases.length)return alert('Generate at least one chart first.');if(!window.jspdf)return alert('PDF library is not loaded.');const {jsPDF}=window.jspdf;const doc=new jsPDF({orientation:'landscape',unit:'mm',format:'a4'});doc.setFontSize(18);doc.text('DataInsightPro — Complete Charts Report',14,15);doc.setFontSize(9);doc.text(new Date().toLocaleString(),14,22);canvases.forEach((cv,i)=>{if(i>0)doc.addPage();doc.setFontSize(13);doc.text(`Chart ${i+1}`,14,31);doc.addImage(cv.toDataURL('image/png',1),'PNG',14,38,269,145)});doc.save('DataInsightPro_All_Charts.pdf')}
function downloadAnalysisExcel(){const a=currentAnalysis();if(!a)return alert('Upload and analyze a file first.');if(!window.XLSX)return alert('Excel library is not loaded.');syncAll();const wb=XLSX.utils.book_new();const raw=a.data.map(r=>Object.fromEntries(a.headers.map((h,i)=>[h,r[i]])));XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(raw),'Data');const summaries=multi.map((c,i)=>{const rows=aggregate(a,c.x<0?null:c.x,c.ys,c.agg,c.limit);return rows.map(r=>({Chart:`Chart ${i+1}`,Category:r.label,...Object.fromEntries(c.ys.map((y,k)=>[a.headers[y],r['v'+k]))}))}).flat();if(summaries.length)XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(summaries),'Chart Summary');XLSX.writeFile(wb,'DataInsightPro_Analysis.xlsx')}
async function downloadZip(){syncAll();renderAll();const canvases=[...document.querySelectorAll('#multiChartsHost canvas')];if(!canvases.length)return alert('Generate at least one chart first.');if(!window.JSZip)return alert('ZIP library is not loaded.');const zip=new JSZip();canvases.forEach((cv,i)=>zip.file(`Chart_${i+1}.png`,cv.toDataURL('image/png').split(',')[1],{base64:true}));const blob=await zip.generateAsync({type:'blob'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='DataInsightPro_All_Charts.zip';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(a.href),1000)}
const original=window.renderAnalysis;if(typeof original==='function'){window.renderAnalysis=function(a,f,k){original(a,f,k);setTimeout(()=>{if(a&&a.numericCols?.length)mount()},120)}}
window.addEventListener('load',()=>setTimeout(()=>{if(currentAnalysis()?.numericCols?.length)mount()},200));
const tools=`<section id="proStaticTools" class="section container"><div class="pro-tools-static"><div><p class="eyebrow">BUSINESS FORM</p><h2>Analysis Request Form</h2><p class="muted">Send your data-analysis requirement with an attachment.</p></div><form class="file-form" action="https://formsubmit.co/mohammad.ali.official@hotmail.com" method="POST" enctype="multipart/form-data"><input type="hidden" name="_subject" value="DataInsightPro Analysis Request"><input type="hidden" name="_captcha" value="false"><label>Name<input name="name" required></label><label>Email<input type="email" name="email" required></label><label>Company / Department<input name="company"></label><label>Analysis requirement<textarea name="requirement" rows="4" required></textarea></label><label>Attach file<input type="file" name="attachment"></label><button class="btn primary" type="submit">Submit Analysis Request</button></form></div><div id="calculator" class="pro-tools-static"><div><p class="eyebrow">CALCULATOR</p><h2>Complete Analytics Calculator</h2><p class="muted">Totals, percentage, change, average, ratio and more.</p></div><div class="calc-grid"><label>Value A<input id="calcA" type="number" step="any" placeholder="100"></label><label>Value B<input id="calcB" type="number" step="any" placeholder="25"></label><label>Operation<select id="calcOp"><option value="sum">A + B</option><option value="sub">A − B</option><option value="mul">A × B</option><option value="div">A ÷ B</option><option value="pct">B as % of A</option><option value="change">% change A → B</option><option value="avg">Average A & B</option><option value="ratio">A : B ratio</option><option value="abs">Absolute difference</option></select></label><button class="btn primary" id="calcBtn" type="button">Calculate</button><div id="calcResult" class="calc-result">Result will appear here.</div></div></div></section>`;
window.addEventListener('load',()=>{const page=document.querySelector('main');if(page&&!document.getElementById('proStaticTools')){const d=document.createElement('div');d.innerHTML=tools;page.appendChild(d);document.getElementById('calcBtn').onclick=()=>{const A=Number(document.getElementById('calcA').value),B=Number(document.getElementById('calcB').value),op=document.getElementById('calcOp').value;let r='';if(!Number.isFinite(A)||!Number.isFinite(B))r='Enter valid values.';else if(op==='sum')r=A+B;else if(op==='sub')r=A-B;else if(op==='mul')r=A*B;else if(op==='div')r=B===0?'Cannot divide by zero.':A/B;else if(op==='pct')r=A===0?'Cannot calculate percentage with A = 0.':`${(B/A*100).toFixed(4)}%`;else if(op==='change')r=A===0?'Cannot calculate change from A = 0.':`${((B-A)/A*100).toFixed(4)}%`;else if(op==='avg')r=(A+B)/2;else if(op==='ratio'){if(B===0)r='Cannot calculate ratio with B = 0.';else{const g=(x,y)=>y?g(y,x%y):x;const d=g(Math.abs(A),Math.abs(B));r=`${A/d} : ${B/d}`}}else if(op==='abs')r=Math.abs(A-B);document.getElementById('calcResult').textContent=typeof r==='number'?r.toLocaleString(undefined,{maximumFractionDigits:6}):r}}});
})();